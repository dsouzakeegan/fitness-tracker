import React from "react";

function CustomLayout() {
    
  const layoutStyle = {
    overflow: "hidden",
    width: "100%",
  };

  return <Layout style={layoutStyle}></Layout>;
}

export default CustomLayout;
